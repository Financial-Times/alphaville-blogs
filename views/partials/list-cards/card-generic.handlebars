<div class="alphaville-card-container">
	<div class="alphaville-card alphaville-card--list {{#if isSeriesArticle}} alphaville-card--series{{/if}}{{#if isAlphachat}} alphaville-card--alphachat{{/if}}{{#if isPodcast}} alphaville-card--podcast{{/if}}" data-o-component="o-card" data-article-id="{{id}}">
		<div class="alphaville-card__content">


			{{#unless @root.streamFlag}}
			<div class="alphaville-card__theme">
				<ul class="alphaville-card--inline-list">
					{{#primaryTheme}}
						<li>
							<a href="{{url}}" class="alphaville-card__tag" data-trackable="theme">{{label}}</a>
						</li>
					{{/primaryTheme}}
				</ul>
			</div>
			{{/unless}}
			<h3 class="alphaville-card__heading">
				<a href="{{av2WebUrl}}" data-trackable="heading">{{title}}</a>
			</h3>

			{{#unless @root.streamFlag}}
				{{#seriesArticles}}
					{{> series-articles }}
				{{/seriesArticles}}
			{{/unless}}

				{{#if withImage}}
					{{^rhr}}
						<div class="alphaville-card__image">
		        	<a href="{{av2WebUrl}}" data-trackable="image"><img src="{{image mainImage.url 740 'medium'}}" alt="{{mainImage.title}}" /></a>
						</div>
						{{> grid-cards/elements/standfirst}}
					{{/rhr}}

					<div class="alphaville-card__footer">
						{{> comment-counter}}
						{{> timestamp}}
						{{#byline}}
							{{> byline}}
						{{/byline}}
					</div>
				{{else}}
					{{^rhr}}
						{{> grid-cards/elements/standfirst}}
					{{/rhr}}

					<div class="alphaville-card__footer">
						{{> comment-counter}}
						{{> timestamp}}
						{{#byline}}
							{{> byline}}
						{{/byline}}
					</div>
				{{/if}}

		</div>
	</div>

	{{> list-cards/inlineRead}}
</div>
